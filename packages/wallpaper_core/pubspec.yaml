name: wallpaper_core
description: Core wallpaper engine for AI Weather Wallpaper — renders images, video, Lottie animations, and GLSL shaders.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
