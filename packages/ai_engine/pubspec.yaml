name: ai_engine
description: AI engine for weather wallpaper generation – prompt templates, image generation, caching, upscaling, video generation, and LLM service.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
